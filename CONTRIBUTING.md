# CONTRIBUTING（維護指南）

> 本 repo 為建築師 80150 考試知識庫。維護時請遵守以下，確保「單一來源、可查證、可長期維護」。

## 七大原則
1. **不刪除既有內容**，以重構與抽取共用為主。
2. **DRY 單一來源**：法規/名詞只在 `references/` 維護一次，他處用「詳見 references/…」。
3. 新增檔一律 **Markdown**；新增資料庫放 `references/`（法規、名詞）。
4. 交叉引用一律 **相對路徑**。
5. 保留學習順序 **00→15**；歷屆完整解題放 `敷地計畫與都市設計/歷屆完整解題/`。
6. 每次修改同步更新 **README、INDEX、CHANGELOG**。
7. **不自行改寫法規內容**；法規以全國法規資料庫最新版為準並保留版本/可信度標記（見 09）。

## 如何新增「某年考題完整解題」
1. 在 `歷屆完整解題/` 新增 `<年>-<題名>.md`，沿用既有 11 段架構（見任一年檔）。
2. 檔頂加 **YAML frontmatter**（year/topic/laws/difficulty/keywords/updated/ai_generated）。
3. 檔頂加 **標籤行**（🏷題眼／⚖法規／🛠手法）。
4. 法規一律引 `references/laws/`；不重寫法規內容。
5. 更新 `歷屆完整解題/README.md`（各年表、關鍵法源、標籤總表、申論小圖）、`INDEX.md`、`CHANGELOG.md`。

## 如何新增/修改法規
- 只改 `references/laws/<法規>.md`；標 `最後查證`、`可信度`、`待查`。
- 數字/條號先對全國法規資料庫；不確定寫「待查」、不要猜。

## 如何新增題眼/名詞/手法
- 題眼 → `敷地計畫與都市設計/10-高頻題眼庫.md`
- 名詞 → `references/名詞字典.md`
- 基地特徵→主軸 → `敷地計畫與都市設計/11-設計題英雄條件庫.md`

## 提交前檢查
- [ ] 相對連結無 404（見 `.github/workflows/markdown-check.yml`）
- [ ] README/INDEX/CHANGELOG 已同步
- [ ] 法規標可信度、未捏造條號
